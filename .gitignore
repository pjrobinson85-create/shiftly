# Dependencies
node_modules/

# Build output
server/dist/
client/dist/

# Environment
.env
.env.local

# Prisma
# Migrations are version history — they must be tracked. Only the local db file is ignored.
# server/prisma/migrations/  (do NOT ignore — CI needs this to build the schema)
*.db
dev.db-journal

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
.google-oauth.json
uploads/
